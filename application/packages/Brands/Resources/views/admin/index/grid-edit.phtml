<?php

if (is_allowed('Brands\Controller\Admin\Index@edit')) {
    echo '<a href="' . route(\null, ['action' => 'edit', 'id' => $this->item['id']]) . '">' . $this->item['name'] . '</a>';
} else {
    echo $this->item['name'];
}